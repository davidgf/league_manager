<%= nested_form_for(@league) do |f| %>
  <% if @league.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@league.errors.count, "error") %> prohibited this league from being saved:</h2>

      <ul>
      <% @league.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :teams %><br />
    <%= f.fields_for :teams %>
    <p><%= f.link_to_add "Add a team", :teams %></p>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
